---
/**
 * Textarea — message libre, multi-lignes.
 * Conforme skill §8 (input-labels, error-placement).
 */
interface Props {
  id: string;
  name: string;
  label: string;
  rows?: number;
  required?: boolean;
  placeholder?: string;
  helper?: string;
  error?: string;
  maxlength?: number;
  class?: string;
}

const {
  id,
  name,
  label,
  rows = 5,
  required = false,
  placeholder,
  helper,
  error,
  maxlength,
  class: className = '',
} = Astro.props;

const helperId = helper ? `${id}-helper` : undefined;
const errorId = error ? `${id}-error` : undefined;
const describedBy = [helperId, errorId].filter(Boolean).join(' ') || undefined;

const baseTextarea =
  'w-full px-4 py-3 rounded-xl bg-blanc-casse text-bleu-nuit placeholder:text-bleu-nuit/40 resize-y transition-colors duration-200 ease-out';
const stateClasses = error
  ? 'ring-1 ring-red-500 focus:ring-2 focus:ring-red-500'
  : 'ring-1 ring-bleu-nuit/15 focus:ring-2 focus:ring-bleu focus:ring-offset-0';
---

<div class:list={['flex flex-col gap-1.5', className]}>
  <label for={id} class="text-sm font-medium text-bleu-nuit">
    {label}
    {required && <span class="text-orange ml-0.5" aria-hidden="true">*</span>}
  </label>
  <textarea
    id={id}
    name={name}
    rows={rows}
    required={required}
    placeholder={placeholder}
    maxlength={maxlength}
    aria-invalid={error ? 'true' : undefined}
    aria-describedby={describedBy}
    class:list={[baseTextarea, stateClasses, 'outline-none']}
  ></textarea>
  {helper && !error && (
    <p id={helperId} class="text-xs text-bleu-nuit/60">{helper}</p>
  )}
  {error && (
    <p id={errorId} role="alert" class="text-xs font-medium text-red-600">{error}</p>
  )}
</div>
