---
import data from '../../../data/templates/seve.json';
---
<section id="top" class="relative h-[100svh] min-h-[640px] w-full overflow-hidden">

  <!-- SIGNATURE : frangipaniers qui flottent doucement -->
  <svg width="0" height="0" style="position:absolute" aria-hidden="true">
    <defs>
      <symbol id="frangipanier" viewBox="-50 -50 100 100">
        <g>
          <ellipse cx="0" cy="-26" rx="14" ry="26" fill="#FAF4EA" opacity="0.92" transform="rotate(0)"/>
          <ellipse cx="0" cy="-26" rx="14" ry="26" fill="#FAF4EA" opacity="0.92" transform="rotate(72)"/>
          <ellipse cx="0" cy="-26" rx="14" ry="26" fill="#FAF4EA" opacity="0.92" transform="rotate(144)"/>
          <ellipse cx="0" cy="-26" rx="14" ry="26" fill="#FAF4EA" opacity="0.92" transform="rotate(216)"/>
          <ellipse cx="0" cy="-26" rx="14" ry="26" fill="#FAF4EA" opacity="0.92" transform="rotate(288)"/>
          <ellipse cx="0" cy="-14" rx="9" ry="13" fill="#CC6B49" opacity="0.55" transform="rotate(0)"/>
          <ellipse cx="0" cy="-14" rx="9" ry="13" fill="#CC6B49" opacity="0.55" transform="rotate(72)"/>
          <ellipse cx="0" cy="-14" rx="9" ry="13" fill="#CC6B49" opacity="0.55" transform="rotate(144)"/>
          <ellipse cx="0" cy="-14" rx="9" ry="13" fill="#CC6B49" opacity="0.55" transform="rotate(216)"/>
          <ellipse cx="0" cy="-14" rx="9" ry="13" fill="#CC6B49" opacity="0.55" transform="rotate(288)"/>
          <circle r="6" fill="#9CAF88" opacity="0.85"/>
        </g>
      </symbol>
    </defs>
  </svg>
  <div class="seve-petals pointer-events-none absolute inset-0 z-10" aria-hidden="true">
    <svg class="frangi" style="left:8%;  top:14%; width:54px;  --d:13s; animation-delay:-1s;" viewBox="-50 -50 100 100"><use href="#frangipanier"/></svg>
    <svg class="frangi" style="left:22%; top:62%; width:68px;  --d:15s; animation-delay:-5s;" viewBox="-50 -50 100 100"><use href="#frangipanier"/></svg>
    <svg class="frangi" style="left:46%; top:22%; width:46px;  --d:12s; animation-delay:-3s;" viewBox="-50 -50 100 100"><use href="#frangipanier"/></svg>
    <svg class="frangi" style="left:64%; top:70%; width:60px;  --d:14s; animation-delay:-7s;" viewBox="-50 -50 100 100"><use href="#frangipanier"/></svg>
    <svg class="frangi" style="left:82%; top:30%; width:52px;  --d:16s; animation-delay:-2s;" viewBox="-50 -50 100 100"><use href="#frangipanier"/></svg>
    <svg class="frangi" style="left:90%; top:78%; width:42px;  --d:13s; animation-delay:-9s;" viewBox="-50 -50 100 100"><use href="#frangipanier"/></svg>
  </div>

  <!-- SIGNATURE : deux lignes qui s'entrelacent -->
  <svg class="absolute bottom-0 left-0 w-full h-32 md:h-40 pointer-events-none z-[5]" viewBox="0 0 1440 160" preserveAspectRatio="none" aria-hidden="true">
    <path class="seve-line seve-line-a" d="M-20,140 C260,40 540,140 720,80 C900,20 1180,120 1460,40" fill="none" stroke="#CC6B49" stroke-width="1.6" stroke-linecap="round" opacity="0.85"/>
    <path class="seve-line seve-line-b" d="M-20,40 C260,140 540,40 720,100 C900,160 1180,60 1460,140" fill="none" stroke="#FAF4EA" stroke-width="1.2" stroke-linecap="round" opacity="0.7"/>
  </svg>
  <img src="/images/wedding/extra-1.webp" alt="Cérémonie de mariage à La Réunion" class="absolute inset-0 w-full h-full object-cover" loading="eager" fetchpriority="high" />
  <div class="absolute inset-0" style="background:linear-gradient(180deg, rgba(58,53,45,0.05) 0%, rgba(58,53,45,0.15) 40%, rgba(58,53,45,0.75) 100%);"></div>
  <div class="absolute inset-0" style="background:linear-gradient(110deg, rgba(58,53,45,0.55) 0%, transparent 55%);"></div>

  <div class="relative h-full max-w-content mx-auto px-6 md:px-10 flex flex-col justify-end pb-20 md:pb-28">
    <p class="text-[#FAF4EA]/85 text-[11px] tracking-[0.32em] uppercase mb-6 flex items-center gap-3" data-reveal>
      <span class="w-10 h-px bg-[#CC6B49]"></span>
      Wedding planner · La Réunion · depuis {data.info.annee}
    </p>
    <h1 class="text-[#FAF4EA] leading-[0.88] tracking-[-0.02em] max-w-[1100px]" style="font-family:'Cormorant Garamond',serif;font-weight:400;font-size:clamp(72px,12vw,184px);" data-reveal>
      Sève. <em class="text-[#FAF4EA]/95" style="font-style:italic;">Wedding planner</em><br/>
      <span class="text-[#CC6B49]/95" style="font-style:italic;font-weight:300;">— La Réunion.</span>
    </h1>
    <p class="mt-8 text-[#FAF4EA]/90 text-[18px] md:text-[22px] max-w-2xl leading-[1.5]" data-reveal>
      Vous dites oui. On s’occupe du reste.
    </p>
    <div class="mt-10 flex flex-wrap items-center gap-6" data-reveal>
      <a href="#contact" class="inline-flex items-center gap-3 h-[56px] px-8 bg-[#FAF4EA] text-[#3A352D] rounded-full text-[14px] font-medium tracking-[0.08em] hover:bg-[#CC6B49] hover:text-[#FAF4EA] transition-all hover:translate-y-[-2px]" style="box-shadow:0 14px 32px rgba(0,0,0,0.25);">
        Réserver un RDV découverte
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
      </a>
      <a href="#formules" class="text-[#FAF4EA] text-[14px] tracking-[0.14em] uppercase underline underline-offset-[8px] decoration-[#CC6B49] decoration-2 hover:text-[#CC6B49] transition-colors">
        Voir nos formules
      </a>
    </div>
  </div>

  <div class="absolute bottom-8 right-6 md:right-10 hidden md:flex items-center gap-3 text-[#FAF4EA]/75 text-[11px] tracking-[0.28em] uppercase">
    <span>Défiler</span>
    <span class="w-12 h-px bg-[#FAF4EA]/40"></span>
  </div>

  <style>
    /* Frangipaniers — flottent doucement */
    .frangi {
      position: absolute;
      height: auto;
      opacity: 0.85;
      transform-origin: center;
      animation: frangi-float var(--d, 14s) ease-in-out infinite;
      filter: drop-shadow(0 6px 14px rgba(58,53,45,0.25));
      will-change: transform;
    }
    @keyframes frangi-float {
      0%   { transform: translateY(0)    translateX(0)    rotate(-6deg); }
      50%  { transform: translateY(-14px) translateX(8px) rotate(6deg); }
      100% { transform: translateY(0)    translateX(0)    rotate(-6deg); }
    }
    /* Lignes entrelacées */
    .seve-line {
      stroke-dasharray: 2400;
      stroke-dashoffset: 2400;
      animation: seve-line-draw 4s cubic-bezier(0.65, 0, 0.35, 1) 600ms forwards;
    }
    .seve-line-b { animation-delay: 1200ms; }
    @keyframes seve-line-draw {
      to { stroke-dashoffset: 0; }
    }
    @media (prefers-reduced-motion: reduce) {
      .frangi { animation: none; opacity: 0.85; }
      .seve-line { animation: none; stroke-dashoffset: 0; }
    }
  </style>
</section>
