---
/**
 * Prestations — liste éditoriale par catégorie, format "carte de salon".
 * Pas d'images, juste typographie soignée + filets verts + pointillés.
 */
import data from '../../../data/templates/salon.json';
const categories = Object.entries(data.prestations);
---

<section id="prestations" class="relative bg-salon-blanc py-20 md:py-32 px-6 md:px-10">
  <div class="max-w-content mx-auto">

    <!-- Header de section -->
    <header class="text-center max-w-2xl mx-auto mb-12 md:mb-16">
      <p class="scroll-reveal flex items-center justify-center gap-3 text-xs font-semibold uppercase tracking-[0.3em] text-salon-vert">
        <span class="inline-block w-8 h-px bg-salon-vert" aria-hidden="true"></span>
        <span>La carte des prestations</span>
        <span class="inline-block w-8 h-px bg-salon-vert" aria-hidden="true"></span>
      </p>
      <h2 class="scroll-reveal mt-5 font-fraunces font-bold text-salon-noir leading-tight" style="font-size: clamp(32px, 5vw, 56px);">
        Soigner, couper,
        <span class="italic font-medium text-salon-vert">colorer.</span>
      </h2>
      <p class="scroll-reveal mt-5 text-salon-noir/70 leading-relaxed">
        Des tarifs clairs, des durées réalistes, et un diagnostic offert avant chaque rendez-vous.
      </p>
    </header>

    <!-- Catégories empilées -->
    <div class="max-w-5xl mx-auto space-y-12 md:space-y-20">
      {categories.map(([key, cat]) => (
        <section class="scroll-reveal">
          <!-- Titre catégorie + filet vert -->
          <header class="mb-6 md:mb-8 text-center">
            <h3 class="font-fraunces italic font-bold text-salon-noir" style="font-size: clamp(24px, 3.5vw, 36px);">
              {cat.titre}
            </h3>
            <div class="mt-3 md:mt-4 mx-auto w-12 h-px bg-salon-vert-clair" aria-hidden="true"></div>
          </header>

          <!-- Items en 2 colonnes desktop, 1 col mobile -->
          <ul class="grid md:grid-cols-2 gap-x-12 gap-y-0">
            {cat.items.map((item) => (
              <li class="group py-5 border-b border-salon-noir/10 last:border-b-0 md:[&:nth-last-child(-n+2)]:border-b-0">
                <div class="flex items-baseline gap-3">
                  <h4 class="font-fraunces font-semibold text-lg text-salon-noir leading-snug">
                    {item.nom}
                  </h4>
                  <span class="flex-1 mb-1.5 border-b border-dotted border-salon-vert-clair/55" aria-hidden="true"></span>
                  <span class="font-fraunces font-bold text-lg text-salon-vert tabular-nums whitespace-nowrap">
                    {item.prix === null ? 'Sur devis' : `${item.prix}€`}
                  </span>
                </div>
                {item.description && (
                  <p class="mt-1.5 text-sm text-salon-noir/70 italic leading-relaxed pr-2">
                    {item.description}
                  </p>
                )}
                <p class="mt-2 text-xs uppercase tracking-[0.18em] text-salon-noir/50">
                  Durée · {item.duree}
                </p>
              </li>
            ))}
          </ul>
        </section>
      ))}
    </div>

    <!-- CTA bas -->
    <div class="scroll-reveal text-center mt-12 md:mt-16">
      <a
        href="#rendez-vous"
        class="inline-flex items-center justify-center h-14 px-8 text-sm font-semibold uppercase tracking-[0.2em] rounded-full bg-salon-vert text-salon-blanc hover:bg-[#1F3328] transition-all"
        style="box-shadow: 0 8px 24px rgb(45 74 58 / 0.25);"
      >
        Réserver un créneau
      </a>
    </div>
  </div>
</section>
