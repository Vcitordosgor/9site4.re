---
import '@fontsource/crimson-pro/latin-400.css';
import '@fontsource/crimson-pro/latin-500.css';
import '@fontsource/crimson-pro/latin-600.css';
import '@fontsource/crimson-pro/latin-400-italic.css';
import '@fontsource/crimson-pro/latin-500-italic.css';
import '@fontsource/caveat/latin-400.css';
import '@fontsource/inter/latin-400.css';
import '@fontsource/inter/latin-500.css';

import BaseLayout from '../../layouts/BaseLayout.astro';
import { getSeo } from '../../lib/seo';
import { localBusinessSchema } from "../../lib/businessSchema";
import data from '../../data/templates/painleve.json';

import Header from '../../components/templates/boulangerie/Header.astro';
import Hero from '../../components/templates/boulangerie/HeroKraft.astro';
import Manifesto from '../../components/templates/boulangerie/Manifesto.astro';
import Four from '../../components/templates/boulangerie/Four.astro';
import Levain from '../../components/templates/boulangerie/Levain.astro';
import Carte from '../../components/templates/boulangerie/Carte.astro';
import Precommande from '../../components/templates/boulangerie/Precommande.astro';
import Calendrier from '../../components/templates/boulangerie/Calendrier.astro';
import Trouver from '../../components/templates/boulangerie/Trouver.astro';
import Faq from '../../components/templates/boulangerie/Faq.astro';
import Footer from '../../components/templates/boulangerie/Footer.astro';

const businessLD = localBusinessSchema(data, 'Bakery');
---

<BaseLayout seo={getSeo('templateBoulangerie')} bare={true}>
  <Fragment slot="head">
    {businessLD && <script type="application/ld+json" set:html={JSON.stringify(businessLD)} />}
    <link rel="preload" as="image" href="/images/boulangerie/hero.webp" fetchpriority="high" />
  </Fragment>

  <Header />
  <main style="background:#F5EBD5; color:#1A1614; font-family:'Crimson Pro',Georgia,serif;">
    <Hero />
    <Manifesto />
    <Four />
    <Levain />
    <Carte />
    <Precommande />
    <Calendrier />
    <Trouver />
    <Faq />
  </main>
  <Footer />

  <a id="bl-sticky" href="#abonnement" data-visible="false"
     class="md:hidden fixed bottom-4 inset-x-4 z-40 inline-flex items-center justify-center h-14 px-6 text-[12px] font-semibold uppercase tracking-[0.22em] rounded-full transition-all duration-300 ease-out data-[visible=false]:opacity-0 data-[visible=false]:translate-y-4 data-[visible=false]:pointer-events-none data-[visible=true]:opacity-100 data-[visible=true]:translate-y-0"
     style="background:#1A1614; color:#C89B3C; font-family:Inter,system-ui,sans-serif; box-shadow:0 12px 32px rgba(26,22,20,0.35);">
     Précommander le pain
  </a>
  <script>
    const s = document.getElementById('bl-sticky');
    const t = document.getElementById('abonnement') || document.getElementById('trouver');
    if (s) {
      const u = () => {
        const past = window.scrollY > 480;
        let inSec = false;
        if (t) { const r = t.getBoundingClientRect(); inSec = r.top < window.innerHeight && r.bottom > 80; }
        s.dataset.visible = String(past && !inSec);
      };
      u(); window.addEventListener('scroll', u, { passive: true }); window.addEventListener('resize', u, { passive: true });
    }
  </script>
</BaseLayout>
