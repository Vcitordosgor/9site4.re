---
import '@fontsource/cormorant-garamond/latin-400.css';
import '@fontsource/cormorant-garamond/latin-500.css';
import '@fontsource/cormorant-garamond/latin-400-italic.css';
import '@fontsource/cormorant-garamond/latin-500-italic.css';
import '@fontsource/plus-jakarta-sans/latin-400.css';
import '@fontsource/plus-jakarta-sans/latin-500.css';
import '@fontsource/plus-jakarta-sans/latin-600.css';

import BaseLayout from '../../layouts/BaseLayout.astro';
import { getSeo } from '../../lib/seo';
import { localBusinessSchema } from '../../lib/businessSchema';
import frangipaneData from '../../data/templates/frangipane.json';

import Header from '../../components/templates/fleuriste/Header.astro';
import HeroPoudre from '../../components/templates/fleuriste/HeroPoudre.astro';
import Compositions from '../../components/templates/fleuriste/Compositions.astro';
import Saison from '../../components/templates/fleuriste/Saison.astro';
import Livraison from '../../components/templates/fleuriste/Livraison.astro';
import Mariage from '../../components/templates/fleuriste/Mariage.astro';
import Galerie from '../../components/templates/fleuriste/Galerie.astro';
import Atelier from '../../components/templates/fleuriste/Atelier.astro';
import Faq from '../../components/templates/fleuriste/Faq.astro';
import Footer from '../../components/templates/fleuriste/Footer.astro';

const businessLD = localBusinessSchema(frangipaneData, 'Florist');
---

<BaseLayout seo={getSeo('templateFleuriste')} bare={true}>
  <Fragment slot="head">
    {businessLD && <script type="application/ld+json" set:html={JSON.stringify(businessLD)} />}
    <link rel="preconnect" href="https://images.unsplash.com" crossorigin />
    <link rel="preload" as="image" href={frangipaneData.info.photoHero} fetchpriority="high" />
  </Fragment>

  <Header />

  <main class="font-jakarta" style="background-color: #F4E6E0; color: #2A2520;">
    <HeroPoudre />
    <Compositions />
    <Livraison />
    <Saison />
    <Mariage />
    <Galerie />
    <Atelier />
    <Faq />
  </main>

  <Footer />

  <!-- Sticky CTA mobile : commander un bouquet -->
  <a
    id="fr-sticky-cta"
    href="#compositions"
    data-visible="false"
    aria-label="Commander un bouquet à l'atelier"
    class="md:hidden fixed bottom-4 inset-x-4 z-40 inline-flex items-center justify-center gap-2 h-14 px-6 font-jakarta font-semibold text-[12.5px] uppercase tracking-[0.2em] rounded-full transition-all duration-300 ease-out data-[visible=false]:opacity-0 data-[visible=false]:translate-y-4 data-[visible=false]:pointer-events-none data-[visible=true]:opacity-100 data-[visible=true]:translate-y-0"
    style="background-color: #2A2520; color: #F4E6E0; box-shadow: 0 12px 32px rgba(42,37,32,0.35);"
  >
    <span>Commander un bouquet</span>
  </a>

  <script>
    const frSticky = document.getElementById('fr-sticky-cta');
    if (frSticky) {
      const updateFrSticky = () => {
        frSticky.dataset.visible = String(window.scrollY > 520);
      };
      updateFrSticky();
      window.addEventListener('scroll', updateFrSticky, { passive: true });
    }
  </script>
</BaseLayout>
